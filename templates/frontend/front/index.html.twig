{% extends 'base.html.twig' %}

{% block nav %}
    <nav class="p-4 bg-slate-900 shadow-xl fixed top-0 left-0 w-full z-50">
    <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-2xl font-bold text-white">ARGON <span class="text-blue-500">Digital</span></h1>
        <div class="space-x-6 hidden md:inline-block">
            <a href="#servicios" class="text-slate-300 hover:text-white transition-colors">Servicios</a>
            <a href="#proyectos" class="text-slate-300 hover:text-white transition-colors">Proyectos</a>
            <a href="#tecnologias" class="text-slate-300 hover:text-white transition-colors">Tecnologias</a>
            <a href="#contacto" class="text-blue-400 font-bold border border-blue-400 px-4 py-2 rounded-lg hover:bg-blue-400 hover:text-slate-900 transition-all">
                Presupuesto
            </a>
        </div>
    </div>
</nav>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        /* Cuando el cursor entra en el grupo, el span con clase tech-label cambia de color */
        .group:hover .tech-label {
            color: var(--brand-color) !important;
        }
    </style>
{% endblock %}

{% block header %}
    <section class="pt-32 pb-20 text-center container mx-auto">
        <h1 class="text-4xl md:text-6xl font-extrabold">
            Desarrollo de <span class="text-blue-600">Sistemas Web</span> a Medida
        </h1>
        <p class="mt-6 text-lg text-gray-600 max-w-2xl mx-auto">
            Creamos aplicaciones web rápidas, seguras y diseñadas para las necesidades reales de tu negocio.
        </p>
        
    </section>
{% endblock %}

{% block body %}
    {# Aquí van Servicios, Proyectos y Tecnologías #}
    <section id="servicios" class="py-20 bg-gray-200">
        <div class="container mx-auto text-center">
            <h2 class="text-3xl font-bold">Servicios</h2>
            <p class="text-gray-600 mt-2">Soluciones a medida para tu negocio</p>

            <div class="grid md:grid-cols-3 gap-10 mt-12">

                <div class="bg-gray-100 p-8 rounded-xl shadow-lg">
                    <h3 class="text-xl font-bold mb-3">Desarrollo a Medida</h3>
                    <p class="text-gray-600">
                        Sistemas personalizados con Laravel o Symfony: dashboards, turnos, clientes, inventarios.
                    </p>
                </div>

                <div class="bg-gray-100 p-8 rounded-xl shadow-lg">
                    <h3 class="text-xl font-bold mb-3">Mantenimiento y mejoras</h3>
                    <p class="text-gray-600">
                        Optimización de rendimiento, actualizaciones, correcciones y nuevas funcionalidades.
                    </p>
                </div>

                <div class="bg-gray-100 p-8 rounded-xl shadow-lg">
                    <h3 class="text-xl font-bold mb-3">Integraciones & APIs</h3>
                    <p class="text-gray-600">
                        Notificaciones por email, sincronización con sistemas externos, automatización de tareas.
                    </p>
                </div>

            </div>
        </div>
    </section>

    <section id="proyectos" class="py-20 container mx-auto bg-slate-900 text-white px-6 rounded-xl">
        <h2 class="text-3xl font-bold text-center text-blue-500">Proyectos</h2>
        <p class="text-gray-600 text-center mt-2">Algunos de nuestros trabajos reales</p>

        <div class="grid md:grid-cols-2 gap-12 mt-12">

            <!-- Proyecto 1 -->
            <div class="bg-white p-8 rounded-xl shadow-lg text-blue-500">
                <h3 class="text-2xl font-semibold">Sistema de Gestión de Ficha Clínica</h3>
                <p class="mt-3 text-gray-600">
                    Sistema completo para gestión de pacientes, turnos y fichas clínicas.
                </p>
                <p class="mt-2 text-sm text-gray-500">
                    Tecnologías: Laravel, Livewire, MySQL
                </p>
            </div>

            <!-- Proyecto 2 -->
            <div class="bg-white p-8 rounded-xl shadow-lg text-blue-500">
                <h3 class="text-2xl font-semibold">Plataforma de Información Contable</h3>
                <p class="mt-3 text-gray-600">
                    Sistema donde los clientes descargan documentación contable de manera segura.
                </p>
                <p class="mt-2 text-sm text-gray-500">
                    Tecnologías: Symfony, EasyAdmin, PostgreSQL
                </p>
            </div>

        </div>
    </section>
    {# seccion tecnologia #}
        <section id="tecnologias" class="py-20 bg-gray-200">
            <div class="container mx-auto text-center px-6">
                <h2 class="text-3xl font-bold mb-4">Tecnologías</h2>

                <div class="flex justify-center flex-wrap gap-6 mt-12">
                    {% for tech in tecnologias %}
                        <div class="group relative px-8 py-4 bg-white border border-gray-100 rounded-2xl shadow-sm 
                                    hover:shadow-xl hover:-translate-y-2 hover:scale-105 transition-all duration-300 cursor-default overflow-hidden"
                            style="--brand-color: {{ tech.colorHex }};">
                            
                            {# Capa de fondo suave #}
                            <div class="absolute inset-0 opacity-0 group-hover:opacity-10 transition-opacity duration-300"
                                style="background-color: var(--brand-color);"></div>
                            
                            {# Borde dinámico #}
                            <div class="absolute inset-0 border-2 border-transparent group-hover:border-[var(--brand-color)] rounded-2xl transition-all duration-300"></div>
                            
                            <div class="relative z-10 flex items-center justify-center gap-2">
                                {# Texto dinámico #}
                                <span class="text-lg font-bold text-gray-700 transition-colors duration-300 tech-label">
                                    {{ tech.nombre }}
                                </span>

                                {# Icono Dinámico (Flecha que aparece) #}
                                <svg xmlns="http://www.w3.org/2000/svg" 
                                    fill="none" viewBox="0 0 24 24" 
                                    stroke-width="2.5" 
                                    stroke="currentColor" 
                                    class="w-5 h-5 opacity-0 -translate-x-4 group-hover:opacity-100 group-hover:translate-x-0 transition-all duration-300 tech-label">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="m8.25 4.5 7.5 7.5-7.5 7.5" />
                                </svg>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </section>
        {# Fin seccion tecnologia #}

    <section id="contacto" class="py-20 container mx-auto bg-slate-900 text-white px-6 rounded-xl">
        <h2 class="text-3xl font-bold text-center text-blue-600">Contacto</h2>
        <p class="text-gray-600 text-center mt-2">Enviame tu consulta y te respondo a la brevedad</p>

        <form class="max-w-xl mx-auto mt-10 bg-white p-8 rounded-xl shadow space-y-5">
            <input type="text" placeholder="Nombre"
                   class="w-full p-3 border rounded-lg">

            <input type="email" placeholder="Email"
                   class="w-full p-3 border rounded-lg">

            <textarea rows="4" placeholder="Mensaje"
                      class="w-full p-3 border rounded-lg"></textarea>

            <button class="w-full bg-blue-600 text-white p-3 rounded-lg hover:bg-blue-700">
                Enviar
            </button>
        </form>
    </section>
{% endblock %}

{% block footer %}

    <footer class="bg-gray-900 text-white py-12 mt-20">
        <div class="container mx-auto px-6">
            <div class="grid md:grid-cols-3 gap-12 text-center md:text-left items-start">
                
                {# Columna 1: Logo y Eslogan #}
                <div class="flex flex-col items-center md:items-start">
                    <h2 class="text-2xl font-bold text-white mb-4">ARGON <span class="text-blue-500">Digital</span></h2>
                    <p class="text-gray-400 text-sm max-w-xs">
                        Transformando ideas en soluciones digitales robustas y escalables. 
                        Especialista en desarrollo backend y sistemas a medida.
                    </p>
                </div>

                {# Columna 2: Redes Sociales #}
                <div>
                    <h3 class="text-lg font-semibold mb-6 text-blue-400">Sígueme</h3>
                    <div class="flex justify-center md:justify-start space-x-6">
                        <a href="#" class="text-gray-400 hover:text-white transition-colors duration-300">
                            <span class="sr-only">LinkedIn</span>
                            <span class="text-sm font-medium">LinkedIn</span>
                        </a>
                        <a href="#" class="text-gray-400 hover:text-white transition-colors duration-300">
                            <span class="sr-only">GitHub</span>
                            <span class="text-sm font-medium">GitHub</span>
                        </a>
                        <a href="#" class="text-gray-400 hover:text-white transition-colors duration-300">
                            <span class="sr-only">Instagram</span>
                            <span class="text-sm font-medium">Instagram</span>
                        </a>
                    </div>
                </div>

                {# Columna 3: Localización y Contacto #}
                <div>
                    <h3 class="text-lg font-semibold mb-6 text-blue-400">Contacto</h3>
                    <ul class="text-gray-400 space-y-4 text-sm">
                        <li class="flex items-center justify-center md:justify-start">
                            <span class="mr-3">📍</span> 
                            Resistencia, Chaco, Argentina
                        </li>
                        <li class="flex items-center justify-center md:justify-start">
                            <span class="mr-3">📞</span> 
                            +54 362 400-0000
                        </li>
                        <li class="flex items-center justify-center md:justify-start">
                            <span class="mr-3">✉️</span> 
                            hola@argondigital.com
                        </li>
                    </ul>
                </div>

            </div>

            {# Línea de separación y Copyright #}
            <div class="border-t border-gray-800 mt-12 pt-8 text-center text-gray-500 text-xs tracking-wider italic">
                &copy; {{ "now"|date("Y") }} ARGON Digital. Todos los derechos reservados.
            </div>
        </div>
    </footer>

{% endblock %}

{# <style>
    /* Forzamos que el texto use la variable cuando se hace hover en el grupo */
    .group:hover span {
        color: var(--brand-color) !important;
    }
</style> #}